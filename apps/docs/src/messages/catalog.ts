import { message } from '@k8ordo/i18n';

export const searchPlaceholder = message({
  ja: '名前や説明で絞り込む',
  en: 'Filter by name or description',
});

export const noResults = message({
  ja: '一致する項目はありませんでした。',
  en: 'No matching items.',
});
