---
"@k8ordo/ui": minor
---

`Checkbox` に `indeterminate`（一部だけ選ばれている途中の状態。input の `indeterminate` に写るので、読み上げでも一部選択になる）と `labelHidden`（`label` を読み上げだけに渡し、描かない）を、`Table.Row` に `selected`（選ばれている行を地の色で示す）を足しました。
